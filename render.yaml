services:
  - type: web
    name: mcp-defect-assistant-api
    env: python
    pythonVersion: "3.11"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn mcp_server:app --host 0.0.0.0 --port 10000"
    plan: free
    autoDeploy: true
